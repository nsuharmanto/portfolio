<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" viewBox="0 0 512 512" preserveAspectRatio="xMidYMid slice">
  <defs>
    <!-- Radial gradient utama -->
    <radialGradient id="mainGrad" cx="50%" cy="40%" r="80%">
      <stop offset="0%" stop-color="#a855f7" stop-opacity="1"/>   <!-- ungu terang -->
      <stop offset="60%" stop-color="#9333ea" stop-opacity="1"/> <!-- ungu medium -->
      <stop offset="100%" stop-color="#1a1025" stop-opacity="1"/> <!-- ungu gelap -->
    </radialGradient>

    <!-- Gradient gelap kiri bawah (lebih pekat + elips vertikal) -->
    <radialGradient id="darkBottomLeft" cx="0%" cy="100%" r="120%" gradientTransform="scale(0.55,1.4)">
      <stop offset="0%" stop-color="#000000" stop-opacity="1"/>   <!-- pekat hitam -->
      <stop offset="35%" stop-color="#050309" stop-opacity="0.95"/>
      <stop offset="70%" stop-color="#0b0613" stop-opacity="0.7"/>
      <stop offset="100%" stop-color="transparent" stop-opacity="0"/>
    </radialGradient>

    <!-- Gradient gelap kanan atas -->
    <radialGradient id="darkTopRight" cx="100%" cy="0%" r="120%">
      <stop offset="0%" stop-color="#120b1c" stop-opacity="0.95"/>
      <stop offset="70%" stop-color="transparent" stop-opacity="0"/>
    </radialGradient>

    <!-- Glow peach kanan atas -->
    <radialGradient id="peachGlow" cx="95%" cy="5%" r="60%">
      <stop offset="0%" stop-color="#f6a18d" stop-opacity="0.9"/>
      <stop offset="40%" stop-color="#f6a18d" stop-opacity="0.35"/>
      <stop offset="100%" stop-color="transparent" stop-opacity="0"/>
    </radialGradient>

    <!-- Blur filter supaya smooth -->
    <filter id="softBlur" x="-100%" y="-100%" width="300%" height="300%">
      <feGaussianBlur stdDeviation="120"/>
    </filter>
  </defs>

  <!-- Background dasar -->
  <rect width="100%" height="100%" fill="url(#mainGrad)"/>

  <!-- Overlay gelap kiri bawah (lebih pekat) -->
  <rect width="100%" height="100%" fill="url(#darkBottomLeft)" filter="url(#softBlur)"/>

  <!-- Overlay gelap kanan atas -->
  <rect width="100%" height="100%" fill="url(#darkTopRight)" filter="url(#softBlur)"/>

  <!-- Glow peach kanan atas -->
  <rect width="100%" height="100%" fill="url(#peachGlow)" filter="url(#softBlur)"/>
</svg>
